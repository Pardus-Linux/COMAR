property data {

	get {
	    instance res;
	    data = res;
	}
	set {
	    instance res;
	    res = data;
	}
}

method check() {	
	instance res;
	makeinstance new, 'newlycreated';	
	res = new;
	check = res;
	register new;
}

method	run() {
	msg = "I'm new Object";
	debugout(value=msg);
	run = msg;
}
property create {
    get	{
	instance res;
	makeinstance new, 'newlycreated';
	create = new;
    }
}
